# PKB 默认模型推荐清单

> 中文优先。以下推荐按「接入方式」分类，均通过「模型管理」界面配置；聊天模型与 Embedding 模型分离设置，互不影响。

## 一、中文 Embedding（核心推荐，中文召回效果验收关键）

| 模型 | 维度 | 接入方式 | 说明 |
|---|---|---|---|
| **BGE-M3** | 1024 | Ollama（`ollama pull bge-m3`）或 OpenAI 协议兼容服务 | 中英多语言、长文本 8192，中文检索 SOTA 级，首推 |
| 智谱 **embedding-3** | 2048（可截断） | OpenAI 协议（base_url `https://open.bigmodel.cn/api/paas/v4`） | 中文效果好，有免费额度 |
| 阿里通义 **text-embedding-v4** | 1024 | OpenAI 协议（DashScope 兼容 `/v1` 或专用兼容地址） | 中文强，检索/聚类双用 |
| 阿里通义 **text-embedding-v3** | 1024 | 同上 | 兼容性好，旧接口通用 |
| OpenAI **text-embedding-3-small** | 1536 | OpenAI 协议 | 英文为主，中文可用 |
| **内置本地向量模型（离线）** | 512 | 系统自带 | 零依赖兜底：无 Key 即可跑通建库→索引→检索全链路；效果有限，追求中文效果请换 BGE-M3 并重建索引 |

**切换步骤**：模型管理 → 添加 Embedding Provider → 连接测试通过 → 设为默认 → 对已有知识库「重建索引」。

## 二、中文对话模型（RAG 回答）

| 模型 | 接入方式 | 说明 |
|---|---|---|
| **DeepSeek-V3 / deepseek-chat** | OpenAI 协议（base_url `https://api.deepseek.com/v1`） | 中文问答性价比之王 |
| 智谱 **GLM-4-Flash** | OpenAI 协议（base_url `https://open.bigmodel.cn/api/paas/v4`） | **免费**，适合快速验收 |
| 阿里通义 **qwen-plus / qwen-max** | OpenAI 协议（DashScope 兼容地址） | 中文稳定，工具调用强 |
| 阿里 **qwen-turbo** | 同上 | 轻量快速 |
| Ollama 本地 **qwen2.5:7b / qwen2.5:14b** | Ollama（地址 `http://localhost:11434`） | 本地离线，免费 |
| Anthropic Claude | Anthropic 原生接入 | 英文/长文档强 |
| Google Gemini | Gemini 原生接入 | 多模态强 |

## 三、多模态（图片入库 / 对话）

多模态能力**由所选模型决定**，系统不内置 OCR / 抽帧：

| 能力 | 推荐模型 | 接入 |
|---|---|---|
| 多模态 Embedding（图片入库检索） | 通义 `text-embedding-v4`（多模态向量）｜ OpenAI 协议兼容的多模态 Embedding 服务 | 知识库配置中把 Embedding 切到多模态模型 |
| 多模态对话（图片问答） | GLM-4V / qwen-vl-max / Gemini / GPT-4o 等 | 聊天模型选多模态模型即可 |

**流程**：知识库设置开启「多模态模式」→ 上传图片/视频（由多模态 Embedding 向量化入库）→ 对话时随问题附图片（发给多模态 LLM）。纯文本模式的知识库上传图片会被明确拒绝并提示。

## 四、图谱抽取模型

抽取需要稳定输出 JSON 的能力：

- 推荐：**GLM-4-Flash**（免费）、**qwen-plus**、**DeepSeek-V3**
- 抽取 Prompt 可在「系统设置 → 知识图谱」中自定义（含宽松 JSON 解析兜底）

## 五、Rerank（可选）

- `hybrid`（默认）：本地混合重排（向量 0.65 + 中文关键词 BM25 风格 0.35），零依赖，已内置，无需额外模型。
- `llm`：使用默认聊天模型对候选做相关性重排（效果更好，消耗 tokens；失败自动回退 hybrid）。
- `none`：关闭重排，直接按向量相似度排序。

## 六、快速验收组合（10 分钟内）

| 角色 | 推荐 | 费用 |
|---|---|---|
| Embedding | 内置本地向量模型（离线） | 免费 |
| 聊天 | 智谱 GLM-4-Flash | 免费 |
| 图谱 | 同聊天模型（GLM-4-Flash） | 免费 |

追求中文检索效果时，将 Embedding 换成 **BGE-M3**（Ollama 本地即可，同样免费）。
